# 项目协作与教材质量规范

本文件适用于当前目录及其所有子目录。所有学科的笔记，包括数学、概率统计、优化、机器学习、深度学习与强化学习，均须按照正式教科书的高标准撰写和排版。新增、补充或修订内容时，应遵守以下约束。

## 总体质量要求

- 以可供系统学习、反复查阅的教科书正文为目标，兼顾数学准确性、逻辑完整性、教学清晰度与版面质量。
- 内容应当能够脱离聊天上下文独立阅读，交代必要的背景、前置知识和符号，不使用“刚才提到”“这句话”等依赖会话的表述。
- 高标准不等于篇幅冗长。根据当前任务确定论述深度；一句定义、一段解释或一个小节也应准确、完整、连贯，不机械增设证明、图表、练习或标题。
- 新内容应融入已有论述，检查上下文衔接和概念重复。概括、直观解释与严格定义应各有作用，避免连续堆叠含义近似的句子；用户明确要求保留原文时，应保留原文并妥善安排位置。
- 修改范围与当前任务一致；调整某一段落时，同时处理由该修改直接引起的符号、交叉引用和分页问题。

## 内容组织与教学表达

- 绪论应交代研究对象、基本问题、必要背景及全书路线，避免提前堆砌后续章节的技术细节。
- 章节按照知识依赖关系组织，由问题和动机引出概念，再根据内容需要展开定义、解释、性质、证明、例子与应用。不同主题可以采用不同结构，不机械套用同一模板。
- 首次出现的重要术语应给出规范中文名称、通行英文名称以及必要的数学符号；没有通行译名时应明确说明，后文用词保持一致。
- 清楚区分直观解释、严格定义、建模假设、数学结论和经验性判断。便于理解的比喻不能代替定义，也不能被写成普遍成立的定理。
- 对容易混淆的概念，应说明各自的对象、条件和区别，必要时给出反例。例如必要条件与充分条件、独立与不相关、最小值与下确界，以及状态与观测、奖励与回报、确定性策略与随机策略。
- 例题和例子应服务于具体概念或方法，说明已知条件、求解思路、关键步骤及结果含义；需要展示适用边界时，可使用反例或退化情形。
- 推导较长时，先说明目标和思路，再分步展开。不得用“显然”“容易证明”等措辞掩盖关键论证。
- 正文使用准确、平实的书面语言，以连贯段落展开论述。列表适合并列要点或步骤；承载独立主题的长篇内容应使用适当的章节层级组织。

## 数学严谨性与符号规范

- 定义应说明所定义对象、适用范围及必要条件；涉及函数、映射、算子和概率模型时，应按需明确其定义域、值域、维度或所在空间。
- 定理、命题与推论应完整写出假设、量词和结论，避免省略会影响成立性的条件。严格区分等价关系、单向蕴含、近似关系与经验规律。
- 证明应具有可核查的逻辑链条，说明关键步骤所用的定义、定理或条件，避免循环论证。仅给证明思路时应明确标注；引用已有结论时，应指出其来源或前文位置并核对适用条件。
- 推导中需要交换极限、积分、求和、微分或期望时，应在适当深度说明使交换合法的条件。涉及存在性、唯一性、收敛性或最优性时，应明确所声称的性质及其前提。
- 每个重要符号应先定义再使用；同一对象保持同一记号，同一语境中避免一符多义。下标、上标、索引范围、向量和矩阵的维度应一致。
- 在概率与统计语境中，随机变量通常使用大写字母，具体取值使用对应的小写字母；时间下标前后一致。期望、概率和条件分布应在需要时明确所依据的分布或随机性来源。
- 写出优化目标时，应说明优化变量、可行域或约束；区分目标函数与算法、总体量与样本估计量、理论最优解与数值近似解。
- 算法说明应按需交代输入、输出、更新规则、初始化、终止条件和适用范围；涉及复杂度或收敛保证时，应写明相应假设。
- 引用外部结论、数据、图表或历史性陈述时，应根据内容需要提供可核验来源。不得编造文献、页码、实验结果或结论归属；尚未核实的内容不得写成已确立事实。

## LaTeX 文件与共享样式

- 每份笔记在项目根目录下使用一个独立的中文文件夹，文件夹名称省略“笔记”，例如 `强化学习/强化学习笔记.tex`；该笔记专用的图片、章节源文件等资源放在其文件夹内。
- 笔记文件名必须使用中文，与文档标题保持一致，并保留 `.tex` 扩展名，例如 `强化学习笔记.tex`。
- 文档标题、章节标题和正文内容可以使用中文。
- 每份笔记必须是可以独立编译的 `.tex` 文档，并通过 `\usepackage{researchnotes}` 使用项目根目录中的 `researchnotes.sty`。
- 笔记导言区通过 `\input@path` 将父目录加入搜索路径，使其从项目根目录或笔记目录编译时均能加载共享样式；不在各笔记文件夹中复制 `researchnotes.sty`。
- 不要重复加载 `researchnotes.sty` 已经提供的页面布局、数学、定理、图表、列表或超链接宏包，避免选项冲突。
- 优先使用共享样式已有的命令和定义、定理、证明、例题等环境，保持语义和视觉风格一致，不用手工加粗或手工编号模拟这些结构。
- 除非任务明确要求，否则不要擅自改变既有文档的字号、纸张尺寸或文档类选项。
- 通用样式问题应优先在共享样式层面统一解决；仅适用于个别对象的排版调整应限制作用范围，避免在各份笔记中复制相互冲突的设置。

## 正文排版

- 正文使用黑色，不在普通叙述中使用彩色高亮。
- 正文中的术语或关键短语使用 `\keyterm{...}`。该命令将中文渲染为楷体、常规字重；强调应克制，不将整段正文设置为强调样式。
- `\textbf{...}` 保留给章节性标题、列表项目标题、定义标题和其他结构性强调，不要用它强调普通正文中的长句。
- 不要重新定义标准命令 `\textbf` 或声明命令 `\bfseries`，以免影响章节标题、定理环境和其他结构。
- 段落缩进、行距和视觉间距由 `researchnotes.sty` 统一控制，不用随意插入 `\vspace`、连续空行、强制换行或手工空格调整版面。
- 中文、英文、数学符号与标点应自然衔接，数学表达式应作为句子的一部分使用相应标点。普通文字使用文本模式，变量和数学关系使用数学模式。
- 避免标题孤悬页尾、段首或段末孤行，以及短语被分页后仅剩几个字的情况；定义、定理和列表的标题应与起始正文保持合理联系。
- 分页应兼顾阅读连续性和页面疏密。优先采用合适的结构、断行与分页策略，避免大量叠加局部补丁、强行保持大段内容同页或拉伸间距造成明显空洞。
- 区分文档类有意保留的章间空白页与排版错误，不为消除正常空白页而擅自更改文档类选项。

## 目录、章节与交叉引用

- 包含目录的独立笔记应在 `\tableofcontents` 后使用 `\clearpage`，确保正文从新的一页开始。
- 正文导言不得直接附着在目录之后，应放入明确的章节或小节中。
- 章节层级应反映实际知识结构，标题准确概括内容，避免跳级或仅为视觉效果创建标题。
- 使用 `\label`、`\ref`、`\eqref` 等机制引用章节、公式、定理和图表，不手工写死编号；标签应有语义且保持唯一。
- 重要或需要引用的公式应编号，临时演算不必逐式编号。目录、正文标题、编号及交叉引用应相互一致。

## 公式、图表与算法排版

- 简短且不妨碍阅读的公式可置于行内；重要结论或较长表达式应独立陈列。长公式使用合适的对齐、分行环境，避免越界或通过过度缩小字号容纳。
- 多步推导应围绕等号或关系符对齐，必要时注明变形依据；公式编号不得与正文重叠。跨页推导应在逻辑上合适的位置断开。
- 表格应有明确的比较维度、列名和必要单位，优先采用简洁的三线表；合理控制列宽和文字长度，不以过小字号换取强行容纳。
- 表格与插图应在正文中被引入并说明其作用，标题准确，位置尽量靠近相关讨论。避免浮动体割裂关键句子、推导或解释段落。
- 跨页长表应使用适当环境并重复表头；表头、图表标题与所对应内容不得脱离。小型对照表应尽量完整呈现。
- 图示应具有教学价值，清楚标明变量、坐标、单位、图例和必要条件。数学示意图优先使用清晰的矢量图，其他图像应有足够分辨率；不依赖颜色作为唯一的信息区分方式。
- 算法或伪代码应层次清楚，步骤、符号和正文定义对应一致，不省略会影响复现的关键操作。

## 编译、审阅与交付

- 修改笔记后，先检查内容的准确性、定义与假设的完整性、符号的一致性及上下文衔接，再检查编译结果。
- 使用 XeLaTeX 编译中文笔记。涉及目录、交叉引用、编号或参考文献时，至少连续编译两次；使用参考文献工具时，应完成所需处理流程并确认引用已稳定。
- 最终交付的 PDF 统一放入项目根目录的 `build/`。可从项目根目录或笔记目录运行 XeLaTeX，通过 `-output-directory` 将编译产物输出至项目根目录下的 `tmp/<学科文件夹名称>/`（如 `tmp/强化学习/`），检查通过后仅将最终 PDF 复制到 `build/`。
- 所有临时文件必须放入项目根目录的 `tmp/`，包括编译中间文件（如 `.aux`、`.log`、`.toc`）、编译输出记录、校验报告、预览图片和临时脚本；可按任务建立子目录，不得散落在项目根目录或 `build/`。
- 检查最终编译日志，不得遗留 LaTeX 错误、未定义引用、缺字或明显的 `Overfull` 警告。出现 `Underfull` 提示时应结合实际版面判断，修复影响阅读的疏松、空洞或分页问题。
- 编译成功不等于排版合格。应查看最终 PDF 中本次改动及其分页影响所涉及的页面，检查目录、标题、公式、图表、页码、孤行和阅读连续性；调整后复查最终版本。
- 修改共享的 `researchnotes.sty` 后，应批量编译所有 `.tex` 文件，并检查受样式变动影响的代表性页面，确认没有破坏其他笔记。
- 仅修改 `AGENTS.md` 等不影响排版的说明文件时，检查规则的完整性和一致性即可，无须重新编译全部笔记。
- 交付时应使源文件与生成的 PDF 对应，简要说明修改位置、主要内容和验证结果；如有尚未解决的问题，应明确指出，不将未完成验证的版本称为已验证完成。
