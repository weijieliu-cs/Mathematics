# 项目协作与教材质量规范

适用于当前目录及所有子目录的学科笔记。以可独立阅读、系统学习和反复查阅的教科书正文为标准，兼顾准确性、逻辑与教学表达；按任务需要确定深度，不机械增加篇幅或结构。

## 工作范围

- 默认只编辑 `.tex` 源文件，负责内容准确性、逻辑、符号、交叉引用及 LaTeX 语法。
- 排版、换行、分页、间距和图表位置由用户自行控制；保留已有的排版设置，不主动调整共享样式或添加排版补丁。
- 除非用户明确要求，不编译或生成 PDF，不将 PDF 转换为图片，不进行截图、页面渲染或视觉检查。

## 内容与数学规范

- 按知识依赖组织章节，由问题和动机引出概念；绪论交代研究对象、背景与全书路线。使用平实、连贯的书面语言，避免依赖聊天上下文。
- 新内容融入已有论述，避免重复；遵守用户保留原文的要求。修改限于任务范围，并处理直接受影响的符号和引用。
- 重要术语首次出现时给出规范中文、通行英文及必要符号，无通行译名时注明。符号先定义后使用，记号、索引和维度保持一致。
- 定义说明对象、范围与条件；定理完整陈述假设、量词和结论。区分直观解释、严格定义、建模假设、数学结论与经验判断，以及等价、蕴含和近似；易混概念按需解释或举反例。
- 证明给出可核查的逻辑与关键依据，不以“显然”等措辞省略关键论证；仅给思路时注明。长推导先交代目标，交换极限、积分、求和、微分或期望，以及声称存在性、唯一性、收敛性或最优性时，说明成立条件。
- 概率统计中区分随机变量与取值（通常分别用大、小写），按需说明分布及随机性来源。优化目标明确变量与约束，区分目标与算法、总体量与样本估计、理论解与数值近似。
- 例题交代条件、思路、关键步骤和结果含义；算法按需给出输入输出、初始化、更新、终止条件与适用范围，复杂度及收敛保证注明假设。
- 引用结论时核对适用条件并指出前文位置或可核验来源；外部数据、图表和历史陈述按需注明来源，不编造文献或将未核实内容写成事实。

## 文件与共享样式

- 每份笔记使用独立中文文件夹，文件夹名省略“笔记”；中文 `.tex` 文件名与文档标题一致，如 `强化学习/强化学习笔记.tex`，专用资源放在同一文件夹内。
- 每份 `.tex` 可独立编译，通过 `\usepackage{researchnotes}` 加载根目录的共享样式，并在导言区用 `\input@path` 加入父目录，确保从根目录或笔记目录编译均可；不复制样式或重复加载其宏包。
- 优先使用共享命令及定义、定理、证明、例题等环境，不手工模拟结构或编号。默认不修改 `researchnotes.sty`；用户明确要求调整样式时再处理。
- 未经任务明确要求，不更改字号、纸张或文档类选项，也不为去除正常章间空白页而更改设置。

## 源文件结构与引用

- 沿用共享样式：正文用黑色，术语或关键短语克制使用 `\keyterm{...}`，`\textbf` 仅用于结构性强调，不重新定义 `\textbf` 或 `\bfseries`。
- 章节层级准确、不跳级，导言置于明确章节中。使用唯一且有语义的 `\label` 及 `\ref`、`\eqref` 引用，不写死编号；重要或需引用的公式编号。
- 文字与数学模式使用恰当，公式按句意加标点；图表在正文中引入，说明含义并标明必要变量、单位和图例。具体分行、对齐和位置由用户调整。

## 检查与交付

- 编辑后检查内容、假设、符号、上下文及源文件语法，包括括号与环境配对、标签和引用的一致性；默认不通过编译或查看页面验证。
- 简述修改位置、主要内容和实际完成的检查。仅编辑源文件即可交付，无须生成或更新 PDF，也无须因省略默认不要求的排版检查而请求确认。
- 用户明确要求编译时，使用 XeLaTeX；涉及目录、交叉引用、编号或参考文献时至少编译两次，并完成所需文献处理。检查日志中的错误、未定义引用和缺字；排版警告可报告，由用户处理。
- 所有临时文件放在根目录 `tmp/`。如需编译，通过 `-output-directory` 输出至 `tmp/<学科文件夹名称>/`；用户要求交付 PDF 时，将最终 PDF 放入根目录 `build/`，并确保与源文件对应。
